// Learn more about moon.mod configuration:
// https://docs.moonbitlang.com/en/latest/toolchain/moon/module.html
//
// To add a dependency, run this command in your terminal:
//   moon add moonbitlang/x
//
// Or manually declare it in `import`, for example:
// import {
//   "moonbitlang/x@0.4.6",
// }

name = "Derk2006/sourcemap"

version = "0.1.0"

readme = "README.md"

repository = "https://github.com/Derk2006/sourcemap.git"

license = "MIT"

keywords = [ "sourcemap", "source-map", "vlq", "compiler", "debug" ]

preferred_target = "wasm"

description = "Pure MoonBit Source Map v3 parser, encoder, validator and lookup toolkit"
